//experimenting with javascript to create a responsive site

let w = window.innerWidth;
let h = window.innerHeight;

function setup() {
  }

  function windowResized() {
    resizeCanvas(windowWidth, windowHeight);
  }
